<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ListViewApplication.TabletNewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ListViewApplication"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="9*"></RowDefinition>
        </Grid.RowDefinitions>

        <!-- Navigation Button -->
        <Button Grid.Row="0" Click="Button_Click" Width="50" Height="50" Background="LightBlue" >
            <SymbolIcon Symbol="Back" Foreground="Green"></SymbolIcon>
        </Button>

        <!-- Content Frame -->
        <Frame Grid.Row="1" x:Name="MainFrame">
            <ContentPresenter x:Name="SurfaceTabletPresentor"></ContentPresenter>
        </Frame>
    </Grid>
</Page>
